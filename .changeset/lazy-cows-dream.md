---
"@lakeql/cli": patch
---

Export `loadBulkConfig` function for testing and add comprehensive test suite for bulk config loading. Supports multiple config file formats (.mjs, .json, .js, .ts) with auto-discovery and explicit paths.
